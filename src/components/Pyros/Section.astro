---
import Container from "./Container.astro";

interface Props {
  id?: string;
  class?: string;
}

const {
  id,
  class: className = "",
} = Astro.props;
---

<section
  id={id}
  class={`py-10 ${className}`}
>
  <Container>
    <slot />
  </Container>
</section>