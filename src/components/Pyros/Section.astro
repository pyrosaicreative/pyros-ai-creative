---
import Container from './Container.astro';

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<section class={`py-10 ${className}`}>
  <Container>
    <slot />
  </Container>
</section>