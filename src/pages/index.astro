---
import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/Pyros/Hero.astro';
import TutorialSections from '~/components/Pyros/TutorialSections.astro';

const metadata = {
  title: 'AI Filmmaking Tutorials & Cinematic AI Workflows',
  description:
    'Learn AI filmmaking with practical tutorials, cinematic AI workflows, consistent AI characters, and production-ready guides.',
};
---

<Layout metadata={metadata}>
  <Hero />
  <TutorialSections />
</Layout>

<script type="module">
  import { supabase } from "/src/lib/supabase.ts";

  const {
    data: { session },
  } = await supabase.auth.getSession();

  console.log("SESSION:", session);
</script>